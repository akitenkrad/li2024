[English](visualization.md) | [日本語](visualization.ja.md)

# Visualization (Python `econagent-tools`)

Install at the workspace root with `uv sync`, then invoke via `uv run econagent-tools <subcommand>`. Dependencies: matplotlib, pandas, numpy, scipy.

## `visualize` — single-run figures

```bash
uv run econagent-tools visualize --results-dir results/latest
```

Reads `metrics.csv` and writes to `{results_dir}/figures/`:

- **`macro_timeseries.png`** — a 2×2 panel of inflation rate, unemployment rate, nominal GDP and the savings Gini coefficient over time. The inflation panel shades the −5%…5% band and the unemployment panel the 2%…12% band (the paper's plausible ranges).
- **`phillips_okun.png`** — two scatter plots with least-squares fit lines and Pearson `r`/`p` (scipy) in the title:
  - **Phillips curve**: unemployment (x) vs inflation (y), restricted to month ≥ 36 (3rd year onward). A negative `r` reproduces the paper's qualitative finding.
  - **Okun's law**: change in unemployment Δu (x) vs nominal-GDP growth (y). A strong negative `r` reproduces Okun's law.

The Pearson `r`/`p` are also printed to stdout.

## `visualize-sweep` — sweep comparison

```bash
uv run econagent-tools visualize-sweep --sweep-dir results/latest
```

Reads `sweep_summary.csv` and writes to `{sweep_dir}/figures/`:

- **`sweep_gini_by_taxscale.png`** — mean final Gini and mean 3-year-onward unemployment by tax-scale (the redistribution effect: higher tax-scale → lower Gini).
- **`sweep_gini_heatmap.png`** — Gini over the (tax-scale × N) grid (only when more than one N is present).
- **`sweep_macro_by_n.png`** — mean inflation/unemployment by N (robustness of the sign/level across agent count).

## Reproduction targets (qualitative)

Because the local default model (`llama3.2`) differs from the paper's `gpt-3.5-turbo`, expect qualitative — not exact — reproduction:

| Quantity | Paper | This reimplementation target |
|---|---|---|
| Phillips correlation | `r = −0.619` (`p < 0.01`) | negative sign |
| Okun correlation | `r = −0.918` (`p < 0.001`) | strong negative sign |
| Inflation (3rd year onward) | stable in −5%…5% | within a plausible band |
| Unemployment | 2%…12% | within a plausible band |

---
*This file was generated by Claude Code.*
