[project]
name = "li2024-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "econagent-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
econagent-tools = { workspace = true }
